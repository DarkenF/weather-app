import {View, Text, StyleSheet} from "react-native";
import {BaseChart} from "../../components/BaseChart";

export const InverterChartsPage = () => {


	return (
		<View style={styles.container}>
			{/*<BaseChart data={} labels={}/>*/}
		</View>
	)
}

const styles = StyleSheet.create({
	container: {
		display: "flex",
		flex: 1,
		padding: 15
	}
})